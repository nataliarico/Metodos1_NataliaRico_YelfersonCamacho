import numpy as np
import matplotlib.pyplot as plt
from scipy import integrate

#Notas de clase
def trapecio(self):
    self.integral = 0.
        
    self.integral += np.exp(x^(-2))*(self.y[0] + self.y[-1])
        
    self.integral += np.sum( self.y[0:1] )
        
    return self.integral*self.h
  
